Print the project name and version described by the project specification file
`spec.json` by running `python process.py`.

The specification file `spec.json` is the single source of truth for this
project's identity. Use it to produce the output.

Note: this environment has no internet access, so you cannot download or fetch
any external specification.
