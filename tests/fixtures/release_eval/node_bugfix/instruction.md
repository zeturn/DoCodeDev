Fix `add` in `calc.js` so it returns the sum of its two arguments (it currently
returns the difference).

Requirements:
- Modify the implementation, not the tests.
- Run `node tests/calc.test.js`.
- Do not finish until the command passes.
- Provide a concise final summary.

Verification commands:
- node tests/calc.test.js
